services:
  s3app:
    image: node:18
    container_name: 68-s3-app
    working_dir: /app
    volumes:
      - ./app:/app
    command: sh -c "npm install && npm run develop"
    ports:
      - "1337:1337"